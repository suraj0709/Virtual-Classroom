import React from "react";

const ClassCard = ({ classItem }) => {
  return (
    <div>
      <h3>{classItem.name}</h3>
      {/* Add functionality to view or manage the class */}
    </div>
  );
};

export default ClassCard;
