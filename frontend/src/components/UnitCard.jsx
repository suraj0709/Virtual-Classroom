import React from "react";

const UnitCard = ({ unitItem }) => {
  return (
    <div>
      <h3>{unitItem.name}</h3>
      {/* Add functionality to view or manage the unit */}
    </div>
  );
};

export default UnitCard;
