import React from "react";

const SessionCard = ({ sessionItem }) => {
  return (
    <div>
      <h3>{sessionItem.name}</h3>
      {/* Add functionality to view or manage the session */}
    </div>
  );
};

export default SessionCard;
